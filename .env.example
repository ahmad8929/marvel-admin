# --- marvels-admin (Refine + Ant Design SPA) ---
# Copy to .env.local for development.

# Production: https://api.marvelsazamgarh.in/api/v1
VITE_API_URL=http://localhost:3001/api/v1
# Only used to build preview URLs; the API already returns absolute media URLs.
VITE_MEDIA_URL=https://api.marvelsazamgarh.in/media
